import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'open-vpn-gui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
